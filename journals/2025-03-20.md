- 00:00
  collapsed:: true
	- [[F: 靈感筆記]]
	- 即便可能失敗也願意花上大量時間去做的，叫毅力；然而就算一百萬個不願意卻始終做得好的，叫作能力
	- 停筆於 00:06
- 01:23
  collapsed:: true
	- 在嘗試解決 git conflict 中
	- 結果，偵查出原因基於 auto_sync.sh 文件出了問題
		- 輸入指令 ``cat /Users/mac/Documents/Sync-Logseq/auto_sync.sh``  後，返回：
			- > #!/bin/bash
			  <br />
			  进入 Sync-Logseq 目录
			  cd /Users/mac/Documents/Sync-Logseq
			  >
			  无限循环，每 15 秒检查一次
			  while true; do
			  >
			  输出当前时间，记录同步开始时间
			   echo "Sync started at $(date)" >> $LOG_FILE
			  >
			  拉取远程仓库的更新
			   echo "Pulling changes..." >> $LOG_FILE
			   git pull origin main >> $LOG_FILE 2>&1
			  >
			  检查是否有更新，并提交
			    commit_message=$(git diff --staged --oneline)
			    if [ -n "$commit_message" ]; then
			        echo "Changes detected, committing..." >> $LOG_FILE
			        git commit -m "Changes: $commit_message" >> $LOG_FILE 2>&1
			        git push origin main >> $LOG_FILE 2>&1
			    else
			        echo "No changes detected" >> $LOG_FILE
			    fi
			  >
			  输出同步完成时间
			  echo "Sync completed at $(date)" >> $LOG_FILE
			  >
			  暂停 15 秒
			  sleep 15
			  >
			  done
		- 問題 1：腳本可能無執行權限
		  logseq.order-list-type:: number
			- 執行指令
			  > chmod +x /Users/mac/Documents/Sync-Logseq/auto_sync.sh
		- 問題 2：git diff --staged 不會檢查未追蹤的文件
		  logseq.order-list-type:: number
		  > commit_message=$(git diff --staged --oneline)
			- 這樣的寫法**只能檢查已經 `git add` 過的變更**，但如果 Logseq 生成了新文件（如 journal），這些文件可能**還沒被 `git add`**，導致腳本不會提交它們。
			- ✅ **修正方式**：
			  > commit_message=$(git status --porcelain)
		- 問題 3：缺少 `git add`，導致新文件未被追蹤
		  logseq.order-list-type:: number
			- 腳本中**沒有 `git add .`**，這意味著 Logseq 生成的新文件（如 journal）不會被 Git 追蹤，自然也不會提交。
			- ✅ **修正方式**：
			  請在 `git commit` 之前加上：
			  > git add .
	- ### 修正後的 `auto_sync.sh`
		- > 进入 Sync-Logseq 目录
		  cd /Users/mac/Documents/Sync-Logseq || exit
		  >
		  设置日志文件
		  LOG_FILE="/Users/mac/Documents/Sync-Logseq/sync_log.txt"
		  >
		  无限循环，每 15 秒检查一次
		  while true; do
		    echo "Sync started at $(date)" >> "$LOG_FILE"
		  >
		  拉取远程仓库的更新
		    echo "Pulling changes..." >> "$LOG_FILE"
		    git pull origin main >> "$LOG_FILE" 2>&1
		  >
		  添加所有新变更
		    git add .
		  >
		  检查是否有变化
		    commit_message=$(git status --porcelain)
		    if [ -n "$commit_message" ]; then
		        echo "Changes detected, committing..." >> "$LOG_FILE"
		        git commit -m "Auto-sync: $(date)" >> "$LOG_FILE" 2>&1
		        git push origin main >> "$LOG_FILE" 2>&1
		    else
		        echo "No changes detected" >> "$LOG_FILE"
		    fi
		  >
		    echo "Sync completed at $(date)" >> "$LOG_FILE"
		  >
		  暂停 15 秒
		    sleep 15
		  >
		  done
	- 停筆於 01:55
- 醬現在呢？from iPhone
  collapsed:: true
	- 似乎必須將 logseq 從 iPhone 關閉後，shortcuts 的 git push automation 才會成功觸發
	- 再試一次只是將 Logseq minimize 而已呢 ( iPhone )
	- 這次成功了 from Mac
- 為了避免 Mac 端頻頻發生報錯 `The current repository is not synchronized with the remote repository, please check.`，剛將 **logseq-plugin-git** 取消
- 但是，考量到從 Mac 端[[即時]]檢測更新且推送是更好的選擇的
  collapsed:: true
	- 結果將 Logseq-plug-in-git 恢復以後，試圖回到 **auto_sync.sh** 腳本，將 sleep 15 改為 sleep 60
	- 同時，輸入指令 `rm /Users/mac/Documents/Sync-Logseq/.git/hooks/post-checkout` 將其刪除
	- 好像是將衝突解決了
- 最後一次 from iPhone
- 與其限制幾秒內無限循環 auto_sync.sh；現在嘗試透過 fswatch 監聽文件是否有變化，才依序推送到 github 去
- 效果不錯，from iPhone 推送到 Mac 的響應速度要比預期更快一些，大約 11 - 35 秒左右
- 從 Mac 同步到 iPhone 端大約[[需要]] 9 - 24  秒
- 也會可能發生衝突
- 但是比起無限循環的體驗好很多
- 可以了，[[滿足]]了 satisfied
- 停筆於 02:53