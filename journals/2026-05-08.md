- 07:20 因外部聲音醒來，因冷斃醒來，賴床
- 07:40 因鬧鐘醒來，走動，起床，解除鬧鐘，喝水，時間賬
- 07:42 走動，排尿，
- 08:[[14]] 時間帳，調整睡眠記錄
- 08:29 緩衝
- 08:42 聽音樂，入耳式耳機，更新日曆 & [[Wed, 06-05-2026]] 諮商 Therapy: Val #12 (Cont. Jacq 19 JC 46) 紀要
- 09:14 聽音樂，入耳式耳機，咬嘴皮，為騰出空間刪手機上的 Apps
- 09:30 晾乾昨天未徹底曬乾的鞋子，走動，覺察羞愧
- 09:35 頭頂和後腦勺覺壓迫，聽音樂，入耳式耳機，藉助 [[[[Antigravity]] [[IDE]]]] 優化 [[Notion]] [[database]] 之 [[Timelinkr]] 的 [[formula property]] ( prop("  Day   ﹢   Segmented [[Sleep]] Duration  ( * 𝘏𝗈𝗏𝖾𝗋 𝗒𝗈𝗎𝗋 𝖼𝗎𝗋𝗌𝗈𝗋 𝗈𝗇 𝗍𝗁𝖾 𝗀𝗋𝖺𝗉𝗁  )") )
  collapsed:: true
	- lets(
	      /* --- 基礎定義 --- */
	      b, "b", i, "i", c, "c", d, "default", nl, "\n", iSpc, " ", sptr, " ｜ ", dash, " ➜ ", arw, "   ►  ", tdy, " ⚡️ ", caution, " ⚠️ ",
	  /* --- 核心設定 --- */
	      cutoffHour, CutOffSleepHour, /* 跨日分界點，晚上 10 點 */
	  /* --- 視覺元素：星期與日期 --- */
	      weekNum, ifs(Date.formatDate("W") == today().formatDate("W") and Date == Week.map(current.週範圍.dateStart()).first(), "• " + " ".repeat(2) + "This Week".style(c,d) + arw, Date == Week.map(current.週範圍.dateStart()).first(), Week.format().substring(0,3).style(c,d) + arw, ""),
	      thisWeek, Date.formatDate("W") == now().formatDate("W"),
	      today, Date.formatDate("L") == now().formatDate("L"),
	      yDay, Date.formatDate("L") == formatDate(dateSubtract(now(), 1, "days"), "L"),
	      dayOfWeek, formatDate(Date, "ddd"),
	      lastDayOfWeek, "Last " + formatDate(Date, "ddd"),
	      date, ifs( 
	          /* --- 本週：當天 --- */
	          thisWeek and today and weekNum == "", "• " + " " + tdy + "Today".link(now()) + sptr, 
	          thisWeek and today and weekNum != "", tdy + "Today".link(now()) + sptr, 
	          /* --- 本週：昨天 --- */
	          thisWeek and yDay and weekNum == "", "• " + " ".repeat(2) + "Yesterday".link(dateSubtract(now(), 1, "days")) + sptr, 
	          thisWeek and yDay and weekNum != "", "Yesterday".link(dateSubtract(now(), 1, "days")) + sptr, 
	          /* --- 本週：其他日子 --- */
	          thisWeek and !today and !yDay and weekNum == "", "• " + " ".repeat(2) + dayOfWeek + sptr, 
	          thisWeek and !today and !yDay and weekNum != "", dayOfWeek + sptr, 
	          /* --- 上週：顯示 Last [Day] --- */
	          Date.formatDate("W") == formatDate(dateSubtract(now(), 1, "weeks"), "W"), lastDayOfWeek + sptr, 
	          /* --- 歷史數據 (14天前+)：非週一顯示日期 (M/DD) --- */
	          weekNum == "", "(" + Date.formatDate("M/DD") + ")" + sptr, 
	          /* --- 歷史數據 (14天前+)：週一則保持空白，由 weekNum 顯示 W[XX] 標籤 --- */
	          "" 
	      ),
	  /* --- 邏輯核心：計算總睡眠分鐘數 --- */
	      totalMinutes, lets(
	          yesterdayPage, Week.map(current.Day).first().filter(current.Date == Date.dateSubtract(1, "days")).first(),
	          yesterdaysSleepMinutes, if(yesterdayPage.empty(), 0, yesterdayPage.Timelog.filter(current.Habits.contains("睡覺") and current.Start.hour() >= cutoffHour).map(current.Time Elapsed ( min )).sum()),
	          todaysSleepMinutes, Timelog.filter(current.Habits.contains("睡覺") and current.Start.hour() < cutoffHour).map(current.Time Elapsed ( min )).sum(),
	          yesterdaysSleepMinutes + todaysSleepMinutes
	      ),
	  /* --- 主要邏輯：根據是否有睡眠時間來決定輸出 --- */
	      if(
	          totalMinutes > 0,
	          /* 如果有睡眠時間，則計算並顯示時長和時段 */
	          lets(
	              /* 格式化總時長 */
	              formattedDuration, lets(
	                  hours, floor(totalMinutes / 60),
	                  minutes, totalMinutes % 60,
	                  (" " + format(hours) + "h " + format(minutes) + "m ").style(b, c)
	              ),
	- /* 計算並格式化睡眠時段 */
	            hrRange, lets(
	                allDaysInWeek, Week.map(current.Day).first(),
	                allTimelogsInWeek, allDaysInWeek.map(current.Timelog).flat(),
	                relevantLogs, allTimelogsInWeek.filter(
	                    let(
	                        log, current,
	                        logDayPage, log.Day.first(),
	                        logDate, if(logDayPage.empty(), fromTimestamp(0), logDayPage.Date),
	  isSleep, log.Habits.contains("睡覺"),
	                        isYesterdaysLate, logDate == Date.dateSubtract(1, "days") and log.Start.hour() >= cutoffHour,
	                        isTodaysEarly, logDate == Date and log.Start.hour() < cutoffHour,
	                        
	                        isSleep and (isYesterdaysLate or isTodaysEarly)
	                    )
	                ),
	                formattedRanges, relevantLogs
	                    .sort(current.Start)
	                    .map(
	                        current.Start.formatDate("HH:mm") + dash + current.End.formatDate("HH:mm")
	                    ).join(", "),
	                if(formattedRanges.empty(), "", /*" (" +*/ formattedRanges /*+ ")"*/)
	            ),
	            
	            /* 組合輸出 */
	            weekNum + date /*+ formattedDuration*/ + hrRange
	        ),
	        
	        /* 如果沒有睡眠時間，則顯示警告 */
	        weekNum + date + caution
	    )
	  )<!-- notionvc: 8fffd975-f0a4-45d5-ac95-49d6a7dedc91 -->
- [[10]]:20 因眼前事物分心 ── 調整手機的 [[UI]]，為在主頁顯示 [[Timelinkr]] 的 [[sleeping trend graph]]，網搜相關 [[App]]
- [[10]]:48 緩衝，走動，喝水
- [[11]]:01 鞋子收進室內，排尿，走動
- 11:16 OGC，入耳式耳機，暗室直視 3C產品
- [[11]]:50 喝水，走動，吃小食
- [[11]]:55 藉助 [[豆包 AI]] 討論針對 [[綠聯 UGREEN HiTune X8]] 的[[前 30 名付尾款享半價折扣 + 前 200 名付尾款立減 30 元]] 需符合的條件，確認自己極可能據不符時感到難過
	- 結束於 15:20
- 15:21 逛櫥窗，強迫性做事
- 18:01 暫停，喝水，走動
- 18:10 回應對方的需求時也傾瀉自己的情緒，覺察不耐煩
- 21:56 時間帳，覺察太陽穴緊繃，打瞌睡
- 22:27 走動
	- 結束於 [[Sat, 09-05-2026]] 00:[[10]]
-