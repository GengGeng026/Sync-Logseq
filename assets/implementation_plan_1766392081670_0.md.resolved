# PKM「Jarvis」系統重建計劃

## 目標
將您現有分散且高摩擦的 Notion/Logseq 架構，轉型為「低認知負荷」的智能系統。核心目標：從「維護工具的奴隸」進化為「駕馭工具的主人」。

## 需要您確認的事項

> [!IMPORTANT]
> **核心轉型**：我們將放棄「手動資料庫管理」。AI (Antigravity/MCP) 將作為「中央處理器」承擔所有複雜度，留給您的只有**極簡介面**。
> **拒绝「流水帳」**：針對 Logseq 裡 633 條姿勢記錄，我不會貿然寫入 Notion 的 Timelinkr。我們將採取「摘要卷積 (Summary Rollup)」策略：Logseq 負責記錄細節，Notion 只記錄每日的「關鍵指標（例如：坐姿總體佔比、長時間未起身異常情形）」。

> [!CAUTION]
> **極簡習慣追蹤**：為避免「坐/躺記錄焦慮」，我們將實現一個 **"State Switch (狀態切換)"** 機制，而不是「逐案記錄」。
> **Notion Schema 保護**：鑑於 Timelinkr 擁有極其複雜的公式 (Formula) 與按鈕 (Button)，我絕對不會寫入非必要的 Property。所有自動寫入都將先與您核對 Schema 對應關係。


---

## 使用者環境確認 ✅

| 項目 | 確認內容 |
|------|----------|
| **主力設備** | Samsung Galaxy A14 (Android) + Tasker/Termux |
| **輔助設備** | iPhone 7 (備用) |
| **Logseq (Mac)** | `/Users/mac/Documents/Sync-Logseq/journals/` |
| **Logseq (Android)** | `Internal Storage/Documents/logseq-termux/journals/` |
| **同步機制** | Github Repo (雲端 + 本地) |
| **授權項目** | 允許開始對 `Discord_Notion_Assistant` 進行重組 |

---

## 系統架構（三層設計）

### 第一層：輸入層（快速且流暢）

#### [重組] [Notion-Intelligence-Core](file:///Users/mac/Desktop/Discord_Notion_Assistant)
- 刪除過時的 `discord_bot.py` 及 Flask 相關程式碼
- 保留並強化 `notion_manager.py`
- 新增 `logseq_scanner.py`：掃描 Logseq journals 中的關鍵標籤

#### [重組] Tasker 配置 (Android)
- **狀態切換 Widget**：取代目前 `Notion_Quick_Log.tsk.xml` 繁瑣的「選擇 KR -> 選擇習慣」對話流。
- **一鍵狀態切換**：例如點擊「坐下」，Tasker 只需在本地發送一個狀態信號，並由 Termux 定時彙總或即時更新「當前狀態」至 Notion 的單一看板。
- **冷靜期 (Cooldown)**：防止強迫性記錄，按鈕點擊後將進入 2 分鐘冷卻，降低「反覆記錄」的焦慮感。


---

### 第二層：處理層（Jarvis 引擎）

#### [新增] `logseq_to_notion_weaver.py`
- 每日掃描 Logseq journals（路徑已確認）
- 識別特定標籤（如 `#therapy`, `#habit`, `#insight`）
- 自動結構化並寫入您的 Notion 資料庫（例如 Timelinkr）

#### [新增] 臨床數據 RAG（檢索增強生成）
- 索引您的 Clinical PDF（PHQ-9、LSAS、Sleep Log）
- 讓您可以用自然語言詢問：
  - *「我上次諮商的 OCPD 結論是什麼？」*
  - *「分析我十月份的睡眠趨勢與焦慮分數」*

---

### 第三層：輸出層（智能鏡像）

#### [新增] 低頻高洞察週報
- 取代每日覆盤的壓力
- 每週生成一份「類人化、溫暖且專業」的狀態總結
- 主動提醒異常模式（例如：連續 3 天睡眠 < 5 小時）

---

## 驗證計劃

### 自動化測試
1. 測試 `logseq_scanner.py` 能否正確讀取您的 journals
2. 測試 Notion 頁面創建是否符合新精簡 Schema

### 手動驗證
1. 您在 Android 上測試「一鍵習慣」按鈕
2. 驗證首份「Jarvis 週報」的同理心與準確度

---

## 下一步行動

- [ ] 執行 `Discord_Notion_Assistant` 重組
- [ ] 創建 `logseq_scanner.py` 原型
- [ ] 設計 Tasker「習慣打卡」配置
